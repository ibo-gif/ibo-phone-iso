# 🚀 IBO Phone ISO - Запуск и установка

## 📱 Что это?

**ibophone.iso** - это полный образ операционной системы IBO Phone, готовый к запуску на виртуальной машине или физическом устройстве.

## ⚡ Быстрый старт (30 секунд)

Самый быстрый способ запустить IBO Phone:

```bash
bash ibophone.sh
```

Выберите в меню:
- `1` - Симуляция (интерактивная демонстрация)
- `2` - QEMU эмулятор
- `3` - Информация об ISO

## 📋 Требования

### Для симуляции (интерактивная демонстрация)
- ✓ Bash
- ✓ Нет дополнительных требований

### Для QEMU эмулятора
```bash
# Ubuntu/Debian
sudo apt-get install qemu-system-arm qemu-system-x86

# Fedora
sudo dnf install qemu-system-arm qemu-system-x86

# macOS
brew install qemu
```

### Для VirtualBox
```bash
# Ubuntu/Debian
sudo apt-get install virtualbox

# Fedora
sudo dnf install virtualbox

# macOS
brew install --cask virtualbox
```

## 🎯 Три способа запуска

### 1️⃣ Интерактивная симуляция (Рекомендуется для быстрого ознакомления)

```bash
bash ibophone.sh
```

Выберите `1` в меню. Это даст вам интерактивное путешествие по IBO Phone OS с симулированным меню, App Store и браузером.

**Преимущества:**
- ✓ Не требует дополнительных программ
- ✓ Быстро и легко
- ✓ Полная интерактивность
- ✓ Демонстрирует все функции

**Недостатки:**
- ✗ Это симуляция, не реальная ОС

### 2️⃣ QEMU эмулятор (Для реальной ОС)

```bash
bash ibophone.sh
```

Выберите `2` в меню. Это запустит IBO Phone в эмуляторе QEMU с реальным Linux kernel.

**Преимущества:**
- ✓ Реальная операционная система
- ✓ Полный Linux kernel
- ✓ Настоящие приложения

**Недостатки:**
- ✗ Требует QEMU
- ✗ Медленнее симуляции

### 3️⃣ VirtualBox (Для графического интерфейса)

```bash
bash run-iso.sh
```

Выберите `2` в меню для запуска через VirtualBox.

## 🔧 Детальные инструкции

### Создание ISO образа

Если ISO еще не создан, он будет создан автоматически при первом запуске:

```bash
bash create-iso.sh
```

Это создаст файл `ibophone-1.0.0.iso.tar.gz` со всей системой.

### Запуск в симуляции

```bash
bash ibophone.sh
# Выберите "1"
```

### Запуск в QEMU

```bash
# Способ 1: Через интерактивное меню
bash ibophone.sh
# Выберите "2"

# Способ 2: Прямая команда
qemu-system-aarch64 -m 2048 -smp 4 -cdrom ibophone-1.0.0.iso.tar.gz -boot c
```

### Запуск в VirtualBox

```bash
# Способ 1: Через скрипт
bash run-iso.sh
# Выберите "2"

# Способ 2: Вручную
VirtualBox &
# Создать новую ВМ, выбрать ibophone-1.0.0.iso как CD привод
```

## 📁 Структура ISO

ISO образ содержит:

```
ibophone-1.0.0.iso
├── boot/                    # Загрузочные файлы
├── system/                  # Системные файлы
│   ├── init.rc             # Инициализация
│   ├── boot.properties     # Параметры загрузки
│   └── package-manager.conf # Конфигурация пакетов
├── apps/                   # Приложения
│   ├── ibo-appstore/      # Магазин приложений
│   └── ibo-browser/       # Веб-браузер
├── packages/              # Пакеты приложений
│   ├── apk/              # APK файлы
│   └── aab/              # AAB файлы
└── documentation/         # Документация и примеры
```

## 🎮 Функции в симуляции

Когда вы запустите симуляцию, вам будет доступно:

### IBO App Store
- Просмотр доступных приложений
- Установка приложений (Chrome, Firefox, Maps, Gmail)
- Управление приложениями

### IBO Browser
- Ввод URL адресов
- Загрузка веб-страниц
- Просмотр содержимого

### Терминал
- Выполнение команд
- ls, ps, df, uname, top
- Просмотр процессов

### Параметры системы
- Информация об ОС
- Характеристики оборудования
- Настройки системы

## 🔐 Параметры виртуальной машины

При запуске в QEMU используются следующие параметры:

| Параметр | Значение |
|----------|----------|
| Память | 2048 MB (2 GB) |
| Процессоры | 4 ядра |
| Архитектура | ARM64 (ARMv8) |
| Диск | Виртуальный 5 GB |
| Kernel | Linux 5.15.0 |

## 📊 Файлы проекта

| Файл | Описание |
|------|---------|
| `ibophone.sh` | Главный скрипт запуска (используйте этот!) |
| `create-iso.sh` | Создание ISO образа |
| `run-iso.sh` | Запуск ISO в QEMU/VirtualBox |
| `ibophone-1.0.0.iso.tar.gz` | Образ системы (создается автоматически) |
| `ibophone-1.0.0.iso.info` | Информация об ISO |

## 🆘 Решение проблем

### Проблема: "QEMU не найден"

**Решение:**
```bash
# Ubuntu/Debian
sudo apt-get install qemu-system-arm

# Или просто используйте симуляцию:
bash ibophone.sh
# Выберите "1"
```

### Проблема: "Недостаточно памяти"

**Решение:** Отредактируйте `ibophone.sh` и измените `-m 2048` на `-m 1024` (или меньше)

### Проблема: "ISO образ очень большой"

**Решение:** ISO сжат в tar.gz формате (~150 KB). Если вам нужен полный ISO:
```bash
tar xzf ibophone-1.0.0.iso.tar.gz
```

### Проблема: "Скрипт не запускается"

**Решение:**
```bash
# Сделайте скрипт исполняемым
chmod +x ibophone.sh
bash ibophone.sh
```

## 📝 Примеры использования

### Пример 1: Быстрая демонстрация

```bash
# Запустить IBO Phone
bash ibophone.sh

# Выбрать "1" для симуляции
# Выбрать "1" для App Store
# Исследовать доступные приложения
```

### Пример 2: Установка приложения

```bash
bash ibophone.sh
# Выбрать "1"
# Выбрать "1" для App Store
# Установить Chrome
```

### Пример 3: Использование браузера

```bash
bash ibophone.sh
# Выбрать "1"
# Выбрать "2" для Browser
# Ввести URL (например, https://example.com)
```

### Пример 4: Работа в терминале

```bash
bash ibophone.sh
# Выбрать "1"
# Выбрать "3" для терминала
# Введите команду (ls, ps, df, uname, top)
```

## 🌟 Возможности

### Текущие возможности
✓ Интерактивная симуляция ОС  
✓ IBO App Store с приложениями  
✓ IBO Browser для веб-серфинга  
✓ Виртуальный терминал  
✓ QEMU эмуляция  
✓ VirtualBox поддержка  
✓ Docker поддержка  

### Планируемые возможности
□ Полная графическая среда  
□ Поддержка сенсорного экрана  
□ Камера и мультимедиа  
□ Телефонные функции (звонки, SMS)  
□ Облачная синхронизация  

## 📄 Лицензия

```
IBO Phone ISO System © 2026
Все права защищены.
```

## 🎓 Обучение

Проект демонстрирует:
- Разработку мобильной ОС
- Управление виртуальными машинами
- Linux kernel конфигурацию
- Bash scripting
- Системы установки приложений

## 🚀 Начать сейчас

Запустите IBO Phone прямо сейчас:

```bash
bash ibophone.sh
```

Это все! Наслаждайтесь IBO Phone OS! 🎉

---

**Вопросы?** Проверьте документацию в проекте или запустите:
```bash
bash ibophone.sh
# Выберите "3" для информации об ISO
```
